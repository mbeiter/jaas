/**
 * Provides a (dummy) implementation of the {@see PasswordAuthenticator} interface, allowing to authenticate a user
 * with a username / password style credential.
 * <p>
 * This implementation is NOT intended for production, as it assumes <code>password = username</code> and is
 * <strong>not secure</strong>
 */
package org.beiter.michael.authn.jaas.loginmodules.password.authenticators.dummy;
